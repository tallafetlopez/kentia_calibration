{
  "name": "calibration-manager",
  "version": "1.0.0",
  "description": "Calibration Manager - HERKO Engineering Tools",
  "author": "HERKO",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . \"Calibration Manager\" --platform=win32 --arch=x64 --out=dist --overwrite --no-asar --icon=icon.ico --extra-resource=../backend/dist/CalibrationManagerServer --extra-resource=mongod.exe --app-version=1.0.0 --build-version=1.0.0 --app-copyright=\"Copyright 2026 HERKO\""
  },
  "devDependencies": {
    "@electron/packager": "^20.0.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.0.0"
  }
}
